# Инструкция по тестированию системы контроля доступа

## Тестовые сценарии

### Сценарий 1: Новый пользователь без реферальной ссылки
1. Запустите бот командой `/start` (без параметров)
2. **Ожидаемый результат**: Появится сообщение "🔒 Нет доступа" с кнопкой "📝 Запросить доступ"
3. Нажмите на кнопку "📝 Запросить доступ"
4. **Ожидаемый результат**: 
   - Появится уведомление "✅ Заявка отправлена!"
   - В админ группу придет сообщение с кнопками "✅ Одобрить" и "❌ Отклонить"

### Сценарий 2: Пользователь с ожидающей заявкой
1. Запустите бот командой `/start` (для пользователя с уже отправленной заявкой)
2. **Ожидаемый результат**: Появится сообщение "⏳ Ваша заявка на доступ находится на рассмотрении."

### Сценарий 3: Одобрение заявки администратором
1. В админ группе найдите сообщение с заявкой пользователя
2. Нажмите кнопку "✅ Одобрить"
3. **Ожидаемый результат**:
   - Сообщение в админ группе обновится и добавится "✅ Заявка одобрена"
   - Пользователю придет уведомление об одобрении
4. Пользователь может теперь использовать бот, отправив `/start`

### Сценарий 4: Отклонение заявки администратором
1. В админ группе найдите сообщение с заявкой пользователя
2. Нажмите кнопку "❌ Отклонить"
3. **Ожидаемый результат**:
   - Сообщение в админ группе обновится и добавится "❌ Заявка отклонена"
   - Пользователю придет уведомление об отклонении
4. Пользователь может снова запросить доступ

### Сценарий 5: Новый пользователь с реферальной ссылкой
1. Перейдите по реферальной ссылке (например: `https://t.me/YourBot?start=ref_123456`)
2. Запустите бот
3. **Ожидаемый результат**:
   - Доступ предоставляется автоматически
   - Показывается приветственное сообщение с кнопками
   - Никаких запросов на доступ не требуется

### Сценарий 6: Проверка обновленного текста
1. Запустите бот с доступом (как пользователь с доступом или по реф. ссылке)
2. **Ожидаемый результат**: В приветственном сообщении должно быть упоминание `@GetGemmsOffershipBot` (не `@GetGemmsNftBot`)

## Проверка файлов

### Проверка data/access_control.json
```bash
cat /workspaces/BotTGPanel/data/access_control.json
```

После добавления пользователя файл должен выглядеть примерно так:
```json
{
  "approved_users": [123456789],
  "pending_requests": []
}
```

## Логи

Следите за логами для отладки:
- `[BOT] [ACCESS]` - логи системы контроля доступа
- `[ACCESS-CONTROL]` - логи работы с файлом доступа
- `[ACCESS-REQUEST]` - логи запросов доступа
- `[ACCESS-APPROVE]` - логи одобрения доступа
- `[ACCESS-REJECT]` - логи отклонения доступа
